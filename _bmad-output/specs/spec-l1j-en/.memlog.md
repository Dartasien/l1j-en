---
topic: l1j-en Lineage 1 server emulator - whole-system build contract
updated: 2026-08-28T14:40
---

- (direction) Create run: adopt the finalized architecture spine (planning-artifacts/architecture/architecture-l1j-en-2026-08-28) as the spec's adopted companion; kernel distilled from spine + its memlog + codebase sweep; purpose = build contract for new features, AI-assisted dev, modernization refactors
- (decision) Slug = l1j-en (inherited from spine name); folder _bmad-output/specs/spec-l1j-en; spine enters companions: as adopted (architecture skill owns it), kernel cites AD-1..AD-7 by id
- (capability) CAP-1 Character & progression: full 2009 US character lifecycle (create, level, skills, equipment, inventory, delete); success: character created on fresh DB levels, equips, and retains all state across a server restart
- (capability) CAP-2 World simulation & combat: maps, monster spawns, movement, skills, drops per configured tables; success: player kills a spawned mob, receives configured drops, respawn honors spawnlist timing
- (capability) CAP-3 Economy & item flow: adena, shops, auction, trade, mail/letters, getback per 2009 US rules; success: item moved between players via trade/auction/mail persists correctly on both sides after restart
- (capability) CAP-4 Social & political structures: parties, clans/pledges, castles incl. siege/war, houses; success: clan captures and holds a castle; war times and ownership persist across restart
- (capability) CAP-5 Timed & recurring events: boss cycles, UB, timed dungeons, light/dark, home-town etc. run on schedule via controllers; success: each configured event fires on schedule with player-visible state transitions
- (capability) CAP-6 GM & operations: in-game GM commands plus telnet/ssh consoles, logging, bans on the live server; success: GM executes each registered command against the live world and ban actions take effect
- (capability) CAP-7 Content pipeline: static content (DB, data/xml, maps, config) loads at boot; changes ship as versioned files stop-the-world; success: a content change shipped per spine AD-4 is live after a stop-the-world restart with zero code changes
- (capability) CAP-8 Client protocol compatibility: unmodified 2009 US client connects, plays, and receives all server features; success: stock client logs in, moves, fights, trades, and uses GM features with no protocol errors
- (constraint) The architecture spine (adopted companion) binds all implementation work: AD-1..AD-7 + consistency conventions; kernel constraints below are its spec-level projection
- (constraint) Wire protocol is a frozen contract with the 2009 US client (spine AD-3); in-memory L1World is runtime authority, content changes stop-the-world (AD-2); single-node Docker Compose envelope (server+MariaDB+nginx), no HA; ratify-as-is scope: build-system/Java-version migration is NOT part of this spec - it amends the spine
- (assumption) The 8 capability domains (CAP-1..8) partition the server's feature surface; derived from the package/content sweep, granularity adjustable by user
- (assumption) Spine AD-3 protocol law holds as written (stock 2009 US client only, no custom-client packet extensions) - flagged for user confirmation during the architecture run, never explicitly confirmed
- (question) Verification policy: no test infra exists; until a test strategy is chosen, is review-against-the-spine the sole verification gate for new work?
- (event) Self-validate pass 1 (coherence, Spec Law 1-6,8): PASS - 8/8 capabilities carry intent+success; intents are WHAT-not-HOW; every constraint rules something out; 5 explicit non-goals; success signal demonstrable (compile + stock client + restart persistence + zero-AD-violation review); CAP ids stable; prose lean
- (event) Self-validate pass 2 (preservation): PASS - all load-bearing spine claims land: AD-1..AD-7 + conventions via adopted companion citation in Constraints; frozen protocol (CAP-8), stop-the-world (CAP-7), single-node envelope, ratify-as-is scope (Constraints + Non-goals); purpose in Why; deferred items in Non-goals/Open Questions; diagrams stay in companion. Wrapper-only drops (spine memlog history, reviewer records) = process metadata, intentionally not preserved
- (decision) User resolved open question 1: fork must support modified clients - spine AD-3 amended in place to 'frozen baseline, gated extensions' (existing packets immutable, stock 2009 US client must keep working, new opcodes for modified clients gated on C_ClientVersion, allocated in Opcodes.java); assumption 'stock client only' retired; Constraints AD-3 citation updated
- (decision) User resolved open question 2: review-against-the-spine is the sole verification gate until a test strategy is adopted - promoted from Open Questions to Constraints (mirrors new spine convention)
- (event) Update run self-validate: PASS - pass 1 coherence: AD-3 citation and new verification-gate constraint bend design; non-goal 'no new protocol features' corrected to match amended AD-3 (client dev out of scope, gated extensions in scope); no open questions remain; assumptions reduced to the capability-partitioning one. Pass 2 preservation: both user resolutions landed (Constraints), retired assumption removed, spine remains the single source for AD detail via companion citation
