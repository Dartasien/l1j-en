---
scope: whole-system: l1j-en Lineage 1 server emulator (Java monolith)
purpose: build-substrate for new features, AI-assisted dev, and modernization refactors
altitude: feature
updated: 2026-08-28T14:19
---

- (direction) Coaching path chosen; spine is primary deliverable (build substrate) for new features, AI-assisted dev, modernization refactors; no extra renderings unless asked
- (constraint) Brownfield: single-JVM Java monolith, ~775 files/~123k LOC, Ant build, packages under src/l1j/server/{server,ssh,telnet}; JDBC datatables+storage; content in data/xml + maps/*.txt; Docker compose (mariadb 12.0.2, nginx, phpmyadmin); targets 2009 US client protocol
- (decision) Spine shape = ratify-as-is: fix invariants the code already obeys; modernization refactors must amend the spine deliberately; no target-state/migration ADs (build system, Java version, structural migration stay Deferred)
- (constraint) Off-main-thread game-state mutation paths are limited to the telnet and SSH GM command interfaces (user trusts this limit; no full sweep commissioned)
- (decision) AD candidate: threading law STRICT - all game-state mutation on the main game thread; telnet/ssh GM command paths are a frozen legacy exception; no new off-thread mutation paths (user confirmed by silence after being offered descriptive alternative)
- (decision) AD candidate: package placement - subpackage-first: new classes go into the matching concern subpackage under l1j.server.server; flat root l1j.server.server is frozen (no new classes directly in it)
- (decision) AD candidate: content placement = follow-the-kind - new static content goes in the same layer as closest existing content of that kind (DB-driven vs data/xml); no moving content between layers; DB schema/content changes ship as versioned SQL in db/ (update_*.sql pattern)
- (decision) AD candidate: dependency policy = permissive-with-rule - new third-party deps allowed but must be vendored into lib/ and wired into build.xml in the same change, noted in the change description; no restriction on which deps
- (decision) AD candidate [ADOPTED, flagged for user review]: protocol law - 2009 US client is frozen; byte layout/semantics of all existing packets immutable; new features may only use opcodes/packets the client already understands (user did not object to the proposed rule)
- (question) Verification/test policy for new code: no test infra exists (no JUnit in lib/, no test sources); user deferred - record as fact, revisit when test strategy is decided
- (decision) AD candidate (SUPERSEDES earlier 'strict main-thread' entry, which was based on upstream l1j and is WRONG for this fork): threading law B - game logic runs concurrently on the 10-thread PacketConsumer pool (+ GeneralThreadPool scheduled/offload, telnet/ssh GM threads); no main-thread guarantee, no global lock; thread safety is per-object synchronized on the object owning the state; new code mutating shared state must lock the object it mutates per the existing per-object pattern; NEW shared mutable state must have an explicit documented lock owner - the legacy no-lock pattern may be inherited but not extended (user chose B)
- (decision) Consistency conventions ratified (user approved the tail): GM commands via command/ package + GMCommands/pcommands.properties registration only; recurring events via controllers/ pattern + GeneralThreadPool scheduling, no raw Timer/sleep loops in feature code; runtime-tunable values in config/*.properties via Config, no hardcoding; slf4j logging only, no System.out; deployment envelope seed = Docker Compose (server + MariaDB + nginx + phpmyadmin), single-node, no HA
- (decision) AD candidate [ADOPTED]: persistence law - L1World in-memory model is the authority at runtime; DB is a snapshot written only by save paths (logout/scheduled/shutdown) through datatables/+storage/; no feature code writes player state straight to DB; no runtime code assumes DB reflects live state; content changes (db/ SQL, data/, maps/) are stop-the-world - applied with server stopped, reloaded on boot (user confirmed stop-the-world is practice)
- (decision) AD-7 added by reviewer gate (adversarial finding): single state owner per feature/event - world/player state in model/ (L1World-owned), event-scoped state in the controller singleton via its accessors (WarTimeController pattern), no parallel copies; AD-4 tightened with new-content-kind tiebreaker (default DB) + next-free update_NNN.sql numbering; conventions gained content-SQL row; stack table annotated with two repo inconsistencies (slf4j-jdk14 1.7.5 in build.xml vs 1.7.25 in lib/; c3p0 in build.xml but no jar in lib/); Deferred gained environments row
- (event) spine finalized: ARCHITECTURE-SPINE.md status=final; reviewer gate passed (lint 0 findings; rubric/adversarial/reality-check reviews in reviews/; all HIGH/MEDIUM findings fixed in-spine); open items triaged: AD-3 protocol law flagged for user confirmation (non-blocking, strict direction); test/verification policy deferred with revisit condition
